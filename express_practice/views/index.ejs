<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div>
        <h1><%= title %></h1>
        <ul>
        <% messages.forEach(msg => { %>
            <li><strong><%= msg.user %>:</strong> <%= msg.text %></li>
        <% }) %>
        </ul>
        <button onclick="window.location='/new'">Add Msg</button>
    </div>
</body>
</html>